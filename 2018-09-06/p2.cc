/* Programa que multiplica dos enters */

#include <iostream>
using namespace std;

int main() {
    int x, y;               // declaració de variables
    cin >> x >> y;          // lectura de les dades
    int p = x * y;          // càlcul del resultat
    cout << p << endl;      // escriptura del resultat
}
