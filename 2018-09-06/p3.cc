/* Programa que suma tres enters */

#include <iostream>
using namespace std;

int main() {
    int x, y, z;            // declaració de variables
    cin >> x >> y >> z;     // lectura de les dades
    int s = x + y + z;      // càlcul del resultat
    cout << s << endl;      // escriptura del resultat
}
